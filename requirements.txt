# Gelistirme ortaminda kurulu surumler (2026-09-06).
# torch burada CPU surumu olarak birakildi: rerank varsayilan KAPALI oldugu icin
# (bkz. config.RERANK_AKTIF) CPU yeterli. GPU icin pytorch.org'un kendi index'ini kullan.

sentence-transformers==6.0.0
torch==2.11.0
numpy==2.5.1
scikit-learn==1.9.0
streamlit==1.63.0
requests==2.34.2
python-dotenv==1.2.2

# dongu.py'nin Tur modeli icin. SURUM SABITLENMEDI: bu makinedeki hicbir
# yorumlayicida kurulu degil, gelistirme ortamindaki surumu okuyamadim.
# Diger satirlar gibi sabitle: pip show pydantic -> pydantic==X.Y.Z
pydantic
